---
---

<nav class="fixed top-11 left-0 right-0 z-50 py-4 backdrop-blur-xl border-b border-accent/10 shadow-sm" id="main-nav" style="background-color: rgba(240, 242, 245, 0.95);">
    <div class="max-w-6xl mx-auto px-6 flex items-center justify-between">
        <a href="/" class="flex items-center gap-2 group">
            <span class="text-xl font-bold text-primary tracking-tight transition-colors duration-300 group-hover:text-accent-indigo" style="font-family: var(--font-heading);">MotivPro</span>
        </a>

        <div class="hidden md:flex items-center gap-8">
            {
                [
                    { name: "Services", href: "/#services" },
                    { name: "Travel", href: "/travel-and-tour" },
                    { name: "Discover", href: "/discover" },
                    { name: "About", href: "/#about" },
                    { name: "Contact", href: "/#contact" },
                ].map((item) => (
                    <a
                        href={item.href}
                        class="text-[13px] font-medium text-primary/60 hover:text-primary transition-all duration-300 relative after:absolute after:bottom-[-4px] after:left-0 after:w-0 after:h-px after:bg-accent-indigo after:transition-all after:duration-300 hover:after:w-full"
                    >
                        {item.name}
                    </a>
                ))
            }
            <a href="/consultation" class="bg-accent-indigo text-white px-6 py-2.5 rounded-full text-[13px] font-semibold hover:shadow-lg hover:shadow-accent-indigo/25 hover:-translate-y-0.5 transition-all duration-300">
                Get Started
            </a>
        </div>

        <button
            class="md:hidden p-2 -mr-2 text-primary"
            id="mobile-menu-toggle"
            aria-label="Toggle Menu"
        >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
        </button>
    </div>

    <div
        id="mobile-menu"
        class="hidden fixed inset-0 z-40 flex flex-col transition-all duration-300"
        style="background-color: #f0f2f5;"
    >
        <div class="px-6 py-4 flex items-center justify-between border-b border-accent/10">
            <span class="text-xl font-bold tracking-tight" style="font-family: var(--font-heading);">MotivPro</span>
            <button id="close-menu" class="p-2 -mr-2">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
            </button>
        </div>
        <div class="flex-1 flex flex-col justify-center items-center gap-8 px-6">
            {
                [
                    { name: "Services", href: "/#services" },
                    { name: "Travel", href: "/travel-and-tour" },
                    { name: "Discover", href: "/discover" },
                    { name: "About", href: "/#about" },
                    { name: "Contact", href: "/#contact" },
                ].map((item) => (
                    <a
                        href={item.href}
                        class="text-3xl font-light text-primary/70 hover:text-primary transition-colors duration-300"
                        style="font-family: var(--font-serif);"
                    >
                        {item.name}
                    </a>
                ))
            }
            <a href="/consultation" class="bg-accent-indigo text-white px-8 py-3 rounded-full text-sm font-semibold hover:shadow-lg hover:shadow-accent-indigo/25 hover:-translate-y-0.5 transition-all duration-300 mt-4" onclick="document.getElementById('mobile-menu').classList.add('hidden')">
                Get Started
            </a>
        </div>
    </div>
</nav>

<script>
    const nav = document.getElementById("main-nav");
    const menuToggle = document.getElementById("mobile-menu-toggle");
    const closeMenu = document.getElementById("close-menu");
    const mobileMenu = document.getElementById("mobile-menu");

    // Mobile menu
    menuToggle?.addEventListener("click", () => {
        mobileMenu?.classList.remove("hidden");
        document.body.style.overflow = "hidden";
    });

    closeMenu?.addEventListener("click", () => {
        mobileMenu?.classList.add("hidden");
        document.body.style.overflow = "";
    });

    mobileMenu?.querySelectorAll("a").forEach((link) => {
        link.addEventListener("click", () => {
            mobileMenu?.classList.add("hidden");
            document.body.style.overflow = "";
        });
    });
</script>
